###############################################
# Produção
# Se frontend e backend estiverem no MESMO domínio:
#   Deixe VITE_API_URL vazio (same-origin) e use proxy/reverse proxy.
# Se backend estiver em outro domínio (ex: api.exemplo.com):
#   Defina VITE_API_URL com a URL completa (https://api.exemplo.com)
# GraphQL pode ser relativo se same-origin.
###############################################
VITE_API_URL=
VITE_GRAPHQL_ENDPOINT=/graphql
